Implement ONLY the Music, Background Music (BGM), and Sound Effects (SFX) pipeline.

Do NOT modify the UI.
Do NOT add any paid API key.
Do NOT require Replicate, SyncLabs, D-ID, or any other new paid provider.
Do NOT modify unrelated image, voice, talking-character, or video-generation code.

First audit the existing music/audio implementation.

Then implement a centralized fallback flow for:

1. AI Music Generation
2. Background Music
3. Sound Effects

Requirements:

- If the primary music provider fails, automatically continue to the next available provider.
- If no external music provider is configured, automatically use the existing local/generated audio fallback where technically possible.
- Never stop the complete video pipeline just because music generation fails.
- Background music must be optional; video generation must still work without BGM.
- Sound effects must be optional; video generation must still work without SFX.
- Validate every generated/downloaded audio file with ffprobe before accepting it.
- Reject missing, zero-byte, corrupt, or zero-channel audio.
- Do not use corrupt files from public/audio.
- Keep the existing valid audio fallback mechanism.
- Make music/BGM/SFX stages sequential:
  provider attempt → validate → accept → continue;
  failure → next provider/fallback.
- Log provider attempts and failure reasons without exposing API keys.
- If multiple valid music/SFX sources are available, select the usable source automatically according to the existing pipeline configuration.
- Do NOT automatically consume unnecessary provider tokens. Use only the provider required for the requested operation.
- Do NOT expose or print any secret/token value.

Integration requirement:

Script → Scene generation → Image/Video assets → Voice/Voiceover → Music/BGM → SFX → FFmpeg composition → MP4 validation.

Music/BGM/SFX must not block the final video when they are optional and unavailable.

Verification:

1. npm run lint
2. npm run build
3. Test primary music provider success.
4. Test provider failure → automatic fallback.
5. Test unavailable music provider → fallback/local path.
6. Test corrupt audio rejection.
7. Test optional BGM failure does not fail video generation.
8. Test optional SFX failure does not fail video generation.
9. Run one end-to-end video composition test.
10. Validate final MP4 with ffprobe for video + audio streams and duration > 0.

DO NOT fix anything outside this task.

At the end report:

- Files changed
- Music provider chain
- BGM fallback chain
- SFX fallback chain
- Token/API keys newly required: YES/NO
- lint: PASS/FAIL
- build: PASS/FAIL
- end-to-end composition: PASS/FAIL
- exact blockers, if any