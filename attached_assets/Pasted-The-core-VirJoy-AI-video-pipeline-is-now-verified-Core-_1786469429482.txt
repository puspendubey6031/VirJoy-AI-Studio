The core VirJoy AI video pipeline is now verified:

- Core Video Pipeline: PASS
- Audio Integration: PASS
- Final MP4: PASS
- Cleanup: PASS
- Tests: 38/38 PASS
- Lint: PASS
- Build: PASS

Talking Character / lip-sync is NOT required for this project.
Do not work on HF LatentSync, SadTalker, lip-sync, or Talking Character.

There is only one remaining implementation issue that should be fixed now:

GLOBAL VIDEO JOB ENGINE

Current problem:
POST /api/v1/jobs/submit with type: "video" creates a project/job record but does not actually execute the production video render. outputUrl is never populated.

Fix this so that the generic video job path uses the same verified production rendering pipeline as:

POST /api/video/plan
→ POST /api/video/render
→ executeFFmpegRender
→ final MP4

Requirements:

1. Audit globalJobEngine and the /api/v1/jobs/submit video path.

2. Make type: "video" execute the real production video-generation/render pipeline.

3. Do NOT create a second independent video renderer.

4. Reuse the existing:
   - workflowEngine
   - VideoComposer
   - executeFFmpegRender
   - BGM pipeline
   - SFX pipeline
   - scene transition system
   - credit enforcement
   - cleanup system
   - MP4 validation

5. Ensure the generic job path correctly:
   - creates the job
   - runs the video pipeline
   - renders the MP4
   - validates the MP4
   - sets outputUrl
   - records the final status
   - deducts credits only after successful rendering
   - refunds credits if rendering fails
   - cleans temporary files

6. Ensure job states are accurate:
   queued → processing → completed
   or
   queued → processing → failed

Never mark a video job "completed" before a valid MP4 actually exists and passes validation.

7. Do not break the existing /api/video/plan and /api/video/render routes.

8. Add automated tests for:
   - generic video job submission
   - actual render execution
   - outputUrl populated after successful render
   - failed render produces failed status
   - credit deduction only after successful render
   - credit refund on failure
   - cleanup after completion/failure
   - existing direct video routes remain functional

9. Run:
   - all existing tests
   - new job-engine tests
   - lint
   - build
   - one real end-to-end generic video job

10. Do not modify the working BGM/SFX/audio pipeline unless a real regression is discovered.

11. Do not work on Talking Character or lip-sync.

IMPORTANT:
Do not fake, mock, or simulate a completed video job.
A job may only become "completed" after a real valid MP4 has been generated and validated.

Final report:

Global Video Job Engine: PASS / FAIL
Real MP4 Generated: PASS / FAIL
outputUrl: PASS / FAIL
Credit Handling: PASS / FAIL
Cleanup: PASS / FAIL
Existing Video Routes: PASS / FAIL
Tests: X/X PASS
Lint: PASS / FAIL
Build: PASS / FAIL
Remaining Blockers: None / List
Files Changed: Complete list